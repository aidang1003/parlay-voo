# This package has no env file of its own.
# Edit /.env at the repo root — it is the single source of truth.
# `packages/foundry/.env` is a symlink to it, installed automatically by
# `pnpm install` (postinstall hook). To re-create the symlink manually,
# run `pnpm setup:env` from the repo root.
